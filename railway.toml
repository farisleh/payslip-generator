[build]
builder = "nixpacks"
buildCommand = "cd apps/api && go build -o main ."
watchPatterns = ["apps/api/**"]

[deploy]
startCommand = "./apps/api/main"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10

[nixpacks]
nixPkgs = ["go"]
